# 🛠️ 创世星环 - 开发工具集

这个目录包含了创世星环项目的开发工具和自动化脚本。

## 📁 工具目录结构

```text
tools/
├── 📦 api-doc-generator/     # API文档生成器
│   ├── package.json         # 包配置
│   └── README.md           # 使用说明
├── 🗂️ generators/           # 代码生成器
│   └── templates/          # 生成器模板
├── 🔌 plugin-generator/     # 插件生成器
│   ├── my-test-plugin/     # 示例插件
│   ├── src/                # 生成器源码
│   └── templates/          # 插件模板
└── 📜 scripts/             # 自动化脚本
```

## 🚀 主要工具

### 🔌 插件生成器 (`plugin-generator/`)

自动化生成VCPToolBox插件的脚手架工具。

#### 插件生成器特性

- **多插件类型支持**: 静态插件、异步插件、服务插件等
- **标准化模板**: 符合项目规范的插件结构
- **测试集成**: 自动生成测试文件
- **类型安全**: 完整的TypeScript类型定义

#### 插件生成器使用方法

```bash
# 安装依赖
cd tools/plugin-generator
npm install

# 生成新插件
npm run generate -- --name my-awesome-plugin --type async

# 运行测试
npm test
```

### 📦 API文档生成器 (`api-doc-generator/`)

从代码注释和类型定义自动生成API文档。

#### API文档生成器特性

- **OpenAPI 3.0规范**: 标准API规范生成
- **交互式文档**: Swagger UI界面
- **类型安全**: 自动生成的TypeScript类型定义
- **多语言支持**: JavaScript、Python SDK生成

#### API文档生成器使用方法

```bash
# 安装依赖
cd tools/api-doc-generator
npm install

# 生成API文档
npm run generate

# 启动文档服务器
npm run serve
```

### 🗂️ 代码生成器 (`generators/`)

通用代码生成器，使用Plop.js模板引擎。

#### 支持的生成器

- **组件生成器**: Vue组件模板
- **服务生成器**: NestJS服务模板
- **测试生成器**: 单元测试模板

#### 代码生成器使用方法

```bash
# 全局安装plop
npm install -g plop

# 运行生成器
plop

# 选择需要的生成器类型
```

## 📜 自动化脚本 (`scripts/`)

### 构建脚本

- **依赖检查**: 自动检测和修复依赖问题
- **类型检查**: TypeScript类型验证
- **代码格式化**: Prettier自动格式化

### 部署脚本

- **Docker镜像构建**: 自动化镜像构建和推送
- **环境部署**: 多环境部署配置
- **回滚脚本**: 快速回滚到上一版本

## 🛠️ 开发工作流

### 1. 新功能开发

```bash
# 生成插件
cd tools/plugin-generator
npm run generate

# 运行测试
npm test

# 构建文档
cd tools/api-doc-generator
npm run generate
```

### 2. 代码质量检查

```bash
# 运行所有检查
pnpm run lint
pnpm run type-check
pnpm test

# 格式化代码
pnpm run format
```

### 3. 部署流程

```bash
# 构建生产镜像
tools/scripts/build-production.sh

# 部署到测试环境
tools/scripts/deploy-staging.sh

# 生产部署
tools/scripts/deploy-production.sh
```

## 📋 贡献指南

### 添加新工具

1. 在相应目录创建新工具
2. 添加 `README.md` 说明文档
3. 更新根目录的 `package.json` scripts
4. 在此文档中添加说明

### 工具命名规范

- 使用 kebab-case 命名
- 清晰描述工具功能
- 添加必要的配置文件

### 测试要求

- 所有工具都需要单元测试
- 集成测试覆盖主要功能
- 文档示例需要验证

## 🔧 维护和更新

### 依赖更新

```bash
# 检查过期的依赖
npm outdated

# 更新依赖
npm update

# 运行兼容性测试
npm test
```

### 文档同步

```bash
# 更新API文档
cd tools/api-doc-generator && npm run generate

# 更新插件模板
cd tools/plugin-generator && npm run update-templates
```

## 📞 支持

如果在使用工具过程中遇到问题：

1. 查看工具目录的 `README.md`
2. 检查项目的主要文档
3. 在GitHub Issues中提交问题
4. 联系开发团队

---

**工具维护**: 请确保在修改工具时同步更新相关文档
